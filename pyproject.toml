[project]
name = "console-games"
version = "0.1.0"
description = "A collection of console games to learn Python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "wordfreq>=3.1.1",
]
